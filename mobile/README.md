# Career Compass Mobile App

Expo-based React Native app that consumes the Career Compass backend.

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```
2. Copy `.env.example` to `.env` (or configure via Expo).
3. Start the development server:
   ```bash
   npx expo start
   ```
